import React from 'react'
import { render } from '@testing-library/react'
import Callout1 from './Callout'

describe('Callout1 Component', () => {
  test('should render', () => {
    render(<Callout1 />)
  })
})
